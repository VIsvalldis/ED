#include <stdio.h>
#include <math.h>


int main (){
	int N, a = 1, count = 0, i = 1;
	printf ("Enter integer N\n");
	scanf ("%d" , &N);
	if (N > 1)
	{
		for (a = 1; a <= N; a++)
			while (i <= a)
			{
				if (a%i==0)
				{
					count++;
				}
				i++;
			}
			if (count == 2)
			{
				printf ("%d", a);
			}
	}
	else 
	{
		printf ("No prime numbers\n");
	}
	return 0;
}
